<li>
  <%= gravatar_for user, :size => 30 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  | <%= link_to "Eliminar", user, :method => :delete, :confirm => "Estas seguro?",
                                :title => "Delete #{user.name}" %>
  <% end %>
</li>
